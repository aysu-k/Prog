// #include "tinyxml2/tinyxml2.h"
#include <string>
#include <iostream>

using namespace std;
// using namespace tinyxml2;

void extract_xml(string file_name, string attribute) {

//	XMLDocument* doc = new XMLDocument;
	//doc->LoadFile(file_name.c_str());

//	if (doc->ErrorID())
//		throw string("XML file " + file_name + "\n" + doc->ErrorStr());
//	else
//		cout << "XML file " << file_name << " has been loaded without errors. :)" << endl;


}
